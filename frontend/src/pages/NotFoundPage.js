import React from 'react';
import { Box } from '@mui/material';

const NotFoundPage = () => (
  <div style={{ padding: 32, textAlign: 'center' }}>
    <h1>404</h1>
    <Box component="span">Страница не найдена</Box>
  </div>
);

export default NotFoundPage;