# README: Сбор и анализ текстового датасета для задач Retrieval-Augmented Generation (RAG)

## Обзор

Этот проект состоит из двух основных скриптов:

1. `parser.py` — парсинг сайта по sitemap.xml и сохранение текстового содержимого.
2. `rag_dataset_analysis.py` — анализ собранных .txt файлов, генерация статистики и визуализаций, полезных для подготовки датасета к задачам Retrieval-Augmented Generation (RAG).

---

## 📂 Установка зависимостей

Убедитесь, что у вас установлен Python 3.9+.

Создайте и активируйте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate    # Windows
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

---

## 🔢 Скрипт парсинга: `parser.py`

### 🌐 Назначение

Извлечение HTML и текста с сайта, ссылки на страницы которого указаны в `sitemap.xml`. Каждая страница сохраняется в виде `.html` и `.txt` файла.

### 🔄 Особенности

* Обход защиты от парсинга (включая время ожидания, заголовки, эмуляцию браузера через Playwright).
* Повторные попытки при неудаче (таймауты, ошибки).
* Статистика времени выполнения.

### 🔹 Использование

```bash
python parser.py
```

### 📁 Выходные данные

* Папка `html/` — сохранённые страницы в HTML
* Папка `text/` — очищенный текст каждой страницы
* Логи выполнения (в консоли)

---

## 📊 Анализ датасета: `rag_dataset_analysis.py`

### 🌐 Назначение

Проводит статистический и лингвистический анализ датасета, подготовленного для RAG:

* подсчёт слов, уникальных слов, символов,
* извлечение частотных слов,
* определение языка,
* расчёт метрик качества (TTR, noise ratio и др),
* визуализация распределений и wordcloud.

### 🔄 Особенности

* Поддержка разных размеров чанков (256, 512, 1024).
* Статистика распределения чанков, дубликатов, языков.
* Генерация wordcloud по частотным словам.

### 🔹 Использование

```bash
python rag_dataset_analysis.py
```

### 📁 Входные данные

* Папка `text/` — результат работы `parser.py`, .txt файлы

### 📁 Выходные данные

* `output/dataset_stats.csv` — таблица с метриками по каждому документу
* `output/dataset_stats.json` — то же в формате JSON
* `output/dataset_summary.json` — сводная статистика по датасету
* `.png`, `.svg`, `.pdf` графики в папке `output/`

Визуализации включают:

* WordCloud
* Распределение количества чанков
* Распределение длин документов
* Шум/структура (paragraph-word ratio, noise ratio)

---

## 🚀 Рекомендации

* Используйте `parser.py` перед запуском анализа.
* Убедитесь, что `sitemap.xml` соответствует структуре сайта.
* Для ускорения анализа используйте tqdm или multiprocessing (по желанию).

---

## 📊 Применимость к задачам RAG

Скрипты позволяют:

* Оценить пригодность датасета для сегментации на чанки.
* Избежать перекрытия тематики и дубликатов.
* Выбрать оптимальный размер чанка с учётом статистики.
* Оценить общий лексический запас и структурные особенности текста.

# Скрипт массовой загрузки файлов для системы RAG

## Описание

Скрипт `mass_upload.py` предназначен для автоматизированной загрузки большого количества текстовых файлов (3000+) в систему RAG. Скрипт автоматически создаёт или использует существующего пользователя, загружает файлы небольшими партиями и контролирует процесс индексации.

## Возможности

- Загрузка большого количества текстовых файлов через API
- Автоматическое создание пользователя, если он не существует
- Контроль нагрузки на сервер с помощью пакетной загрузки
- Отслеживание статуса индексации документов
- Автоматическое возобновление загрузки после прерывания
- Подробное логирование процесса
- Визуальный прогресс-бар

## Требования

- Python 3.8+
- Установленные пакеты:
  - requests
  - tqdm

## Использование

### Базовый запуск

```bash
python dataset_script/mass_upload.py
```

По умолчанию скрипт будет:
- Загружать файлы из директории `dataset_script/text`
- Использовать пользователя `mass_upload` с паролем `Qwerty123!`
- Загружать файлы пакетами по 5 штук
- Ждать 10 секунд между пакетами

### Настройка параметров

```bash
python dataset_script/mass_upload.py --text-dir path/to/files --batch-size 10 --wait-time 15
```

Полный список параметров:

| Параметр | Описание | Значение по умолчанию |
|----------|----------|------------------------|
| `--text-dir` | Директория с текстовыми файлами | `dataset_script/text` |
| `--batch-size` | Количество файлов в одном пакете | 5 |
| `--wait-time` | Время ожидания между пакетами (сек) | 10 |
| `--username` | Имя пользователя | `mass_upload` |
| `--password` | Пароль пользователя | `Qwerty123!` |
| `--resume-file` | Файл для сохранения прогресса | `upload_progress.json` |
| `--retries` | Количество повторных попыток при ошибках | 3 |

## Мониторинг процесса

Во время выполнения скрипт отображает:
1. Общий прогресс загрузки (количество обработанных пакетов)
2. Информацию о текущем пакете
3. Статусы документов на сервере (каждые 5 пакетов)

Подробная информация записывается в файл `mass_upload.log`.

## Возобновление загрузки

В случае прерывания процесса загрузки (сбой, перезапуск и т.д.), скрипт создаёт файл `upload_progress.json`. При повторном запуске скрипт загружает этот файл и продолжает с места остановки, пропуская уже загруженные файлы.

## Пример вывода

```
Загружен прогресс: 150 загруженных файлов, 2 файлов с ошибками
Всего найдено 2712 текстовых файлов
Осталось загрузить 2562 из 2712 файлов
Начинаем загрузку 2562 файлов...
Загрузка пакета 1: ['file1.txt', 'file2.txt', 'file3.txt', 'file4.txt', 'file5.txt']
Файл file1.txt успешно загружен
...
Ожидание 10 секунд перед следующим пакетом...
```

## Примечания

- После загрузки каждых 5 пакетов скрипт проверяет статус индексации и при необходимости ждёт завершения обработки документов
- В конце работы скрипт выводит общую статистику загрузки
- Для остановки скрипта используйте Ctrl+C - он корректно сохранит прогресс
